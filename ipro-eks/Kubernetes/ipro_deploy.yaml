---
apiVersion: v1
kind: Namespace
metadata:
  name: ${IPRO_NAMESPACE}

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: ${IPRO_PV}
spec:
  capacity:
    storage: ${IPRO_PV_SIZE}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: efs-sc
  csi:
    driver: efs.csi.aws.com
    volumeHandle: ${EFS_ID}:/:${EFS_AP}
    volumeAttributes:
      encryptInTransit: "true"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${IPRO_PVC}
  namespace: ${IPRO_NAMESPACE}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  volumeName: ${IPRO_PV}
  selector:
    matchLabels:
      name: ${IPRO_PV}
  resources:
    requests:
      storage: ${IPRO_PV_SIZE}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: ${IPRO_NAMESPACE}
  name: ${IPRO_DEPLOYMENT_NAME}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ${IPRO_APP_NAME}
      namespace: ${IPRO_NAMESPACE}
      alpha.eksctl.io/nodegroup-name: ${IPRO_NODE_GROUP}
  replicas: ${IPRO_MIN_REP}
  revisionHistoryLimit: 4
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ${IPRO_APP_NAME}
        namespace: ${IPRO_NAMESPACE}
        alpha.eksctl.io/nodegroup-name: ${IPRO_NODE_GROUP}
    spec:
      containers:
        - image: ${IPRO_REPOSITORY}:${TAG}
          imagePullPolicy: Always
          name: ${IPRO_APP_NAME}
          ports:
            - containerPort: 8090
          envFrom:
            - secretRef:
                name: ${IPRO_SECRET}
          resources:
            requests:
              cpu: ${IPRO_REQ_CPU}
              memory: ${IPRO_REQ_MEM}
            limits:
              cpu: ${IPRO_LIM_CPU}
              memory: ${IPRO_LIM_MEM}
          livenessProbe:
            failureThreshold: 2
            httpGet:
              path: /
              port: 8090
              scheme: HTTP
            initialDelaySeconds: 120
            periodSeconds: 5
            successThreshold: 1
            timeoutSeconds: 5
          volumeMounts:
            - mountPath: /storage
              name: ${IPRO_VOLUME}
      volumes:
        - name: ${IPRO_VOLUME}
          persistentVolumeClaim:
            claimName: ${IPRO_PVC}
      nodeSelector:
        "eks.amazonaws.com/nodegroup": ${IPRO_NODE_GROUP}

---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: ${IPRO_DEPLOYMENT_NAME}
  namespace: ${IPRO_NAMESPACE}
spec:
  maxReplicas: ${IPRO_MAX_REP} # maximum replicas of pods
  minReplicas: ${IPRO_MIN_REP}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ${IPRO_DEPLOYMENT_NAME} # TARGET name of the deployment the autoscaler need to be run on
  targetCPUUtilizationPercentage: ${IPRO_TGT_CPU_PERCENT} # CPU maximum amount of use on the pod is set to 30%.

---
apiVersion: v1
kind: Service
metadata:
  namespace: ${IPRO_NAMESPACE}
  name: ${IPRO_SERVICE_NAME}
spec:
  ports:
    - port: 80
      targetPort: 8090
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: ${IPRO_APP_NAME}
    namespace: ${IPRO_NAMESPACE}
    alpha.eksctl.io/nodegroup-name: ${IPRO_NODE_GROUP}

# ---
# apiVersion: traefik.containo.us/v1alpha1
# kind: Middleware
# metadata:
#   name: ${IPRO_MIDDLEWARE_NAME}
#   namespace: ${IPRO_NAMESPACE}
# spec:
#   headers:
#     customResponseHeaders:
#       X-XSS-Protection: "1; mode=block"
#       Referrer-Policy: "strict-origin-when-cross-origin"
#       Permissions-Policy: "geolocation=(self), camera=(self), microphone=(self), autoplay=(self)"
#       X-Content-Type-Options: "nosniff"
#       X-Frame-Options: "sameorigin"
#       X-Permitted-Cross-Domain-Policies: "none"
#       Strict-Transport-Security: "max-age=31536000; includeSubdomains; preload"
#       Cross-Origin-Embedder-Policy: "unsafe-none"
#       Cross-Origin-Opener-Policy: "unsafe-none"
#       Cross-Origin-Resource-Policy: "same-origin"
#       Cache-Control: "no-cache, no-store, must-revalidate"

# ---
# apiVersion: traefik.containo.us/v1alpha1
# kind: IngressRoute
# metadata:
#   name: ${IPRO_INGRESSROUTE_NAME}
#   namespace: ${IPRO_NAMESPACE}
# spec:
#   entryPoints:
#     - web
#   routes:
#     - match: Host(`${IPRO_HOST_NAME}`)
#       kind: Rule
#       middlewares:
#         - name: ${IPRO_MIDDLEWARE_NAME}
#       services:
#         - name: ${IPRO_SERVICE_NAME}
#           port: 80

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ${IPRO_NAMESPACE}
  name: ${IPRO_INGRESS_NAME}
  annotations:
    # external-dns.alpha.kubernetes.io/hostname: ${IPRO_HOST_NAME}
    alb.ingress.kubernetes.io/target-type: instance
    alb.ingress.kubernetes.io/certificate-arn: ${IPRO_CERTIFICATE_ARN}
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/load-balancer-name: ${IPRO_ALB}
    alb.ingress.kubernetes.io/load-balancer-attributes: access_logs.s3.enabled=true,access_logs.s3.bucket=ss-prd1-v2-alb-access-logs-new,access_logs.s3.prefix=iproapi-alb,idle_timeout.timeout_seconds=60
    alb.ingress.kubernetes.io/wafv2-acl-arn: ${WAF_ARN}
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    kubernetes.io/ingress.class: ${ING_CLS_NAME}
spec:
  # ingressClassName: ${ING_CLS_NAME}
  rules:
    - host: ${IPRO_HOST_NAME}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${IPRO_SERVICE_NAME}
                port:
                  number: 80
